[phases.setup]
nixPkgs = ["python3", "nodejs", "nginx", "gettext"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd vibe-coding && npm install --production && cd .."
]

[start]
cmd = "bash start.sh"
